# TODO

Pending work only, `- [ ]`, grouped by category, what and why.
Delete an item when it is done — git history is the record.

## Portfolio

- [ ] **Add `px-pixelart` to the portfolio once it has a live origin.** New
  fleet repo (2026-09-25), Pixel Art, a very basic pixel-picture maker. It is
  not deployed yet (its Vercel project is item 1 in that repo's
  `docs/USER_ACTIONS.md`; `https://px-pixelart.vercel.app/` answered 404 on
  2026-09-25), and the audits grade live origins, so an entry now would fail
  `audit:discoverability`. Then follow `docs/PROJECT_DOCS.md` "Adding a New
  Project".
