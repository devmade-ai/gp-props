# TODO

Pending work only, `- [ ]`, grouped by category, what and why.
Delete an item when it is done — git history is the record.

## Portfolio

- [ ] **Add `bl-borderline` to the portfolio once it has a live origin.** New
  fleet repo (2026-09-23), the countries-of-the-world game. It is not deployed
  yet (its Vercel project is a user action in that repo), and the audits grade
  live origins, so an entry now would fail `audit:discoverability`. Then follow
  `docs/PROJECT_DOCS.md` "Adding a New Project".
